DBCONN={'host':'celestial-toymaker','database':orwell-first};class Camera:    camera_id=0;    camera_name="";    camera_port=80;    def __init__(self,id,name,port):        self.camera_id=id;        self.camera_name=name;        self.camera_port=port;    #future    #def __init__(self,id):    #    self.camera_id=id;    #    self.fetchInfoByID();                    def AddScheduleRecord(self,StartAt,Pan,Tilt,Zoom,DBCONN):        SQLCMD="INSERT INTO camera_planning (activationtime,camera_name,pan,tilt,zoom) VALUES ('%s','%s',%s,%s,%s);" %            (StartAt,self.camera_name,Pan,Tilt,Zoom);        os.system("mysql --host=%s -u root -D %s -e '%s' -t -N" % (DBCONN{'host'},DBCONN{'database'},SQLCMD);    def GetConfigs(self,DBCONN):        SQLCMD="SELECT *, '<<' FROM camera_configurations WHERE cameraname='%s';" % self.camera_name;        plainText=os.popen('mysql --host=%s -u root -D %s -e "%s" -t -N  | \                      sed -e "/\+/d" -re "s?\ \|?,?g" -e "s?, <<,.*??" -e "s?\|??"' % (DBCONN{'host'},DBCONN{'database'},SQLCMD) ).readlines();        configs=map(lambda s: s.strip().split(","),plainText);        return configs;