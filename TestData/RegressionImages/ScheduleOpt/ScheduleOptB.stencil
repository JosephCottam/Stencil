stream Renders(Config, Count) from Text["./RenderCounts.csv"]

layer Chart
  guide axis from X
        axis[stride:2] from Y
from Renders
  ID: Config
  X:* Rank(Config) -> Mult(10, _)
  Y: Mult(Count, 5)
  SHAPE: "RECTANGLE"
  HEIGHT: Mult(Count, 5)
  REGISTRATION: "TOP"

