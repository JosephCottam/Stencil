import Layouts
import Geometry : Geo

stream Tweets(message, user, tags, geo, place, date) : Twitter

view
from #Render
  (X,Y,W,H) : (canvas.X, canvas.Y, canvas.W, canvas.H)


layer Map
from Tweets
   filter (geo != NULL)
   ID: Count()
   (X,Y): (geo.1, geo.0) 
   REGISTRATION: "CENTER"
   SIZE: 10
   IMPLANT: "POINT"
   SHAPE: "RECTANGLE"
   FILL_COLOR: Color{Purple, 20}
 
