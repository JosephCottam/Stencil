/**Read from standard in.
   Type single words and follow with newline.
   The "strict:false" is optional, it prevents errors if bad input is found on standard in.
   The "skip:0" is also optional; by default the Text stream assumes a header...which doesn't always happen on std in.
**/
stream survey(fruit) : Text["StdIn", strict:false, skip:0]

layer plot
guide
  axis from X              
from survey
   ID: Count()
   X:* Rank(fruit) -> Mult(5, Rank)
   Y: Count(fruit) -> Mult(5, Count)
   REGISTRATION: "CENTER"
   
