(import CDXRuntime)

(table dataset {source: external}
  (fields a b c))

(table plot
  (renderer scatter)
  (data
    (pull dataset 
          (let (x : a)
               (y : b)
               (color : "RED")))))
