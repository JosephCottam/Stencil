(import CDXRuntime {debug:true})

(table dataset {source: external}
  (fields a b c))

(table plot
  (render scatter)
  (data
    (pull dataset 
          (let (x : a)
               (y : b)
               (color : "RED")))))
