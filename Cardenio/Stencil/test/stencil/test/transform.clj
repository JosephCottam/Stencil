(ns Stencil.test.transform
  (:use [stencil.transform])
  (:use [clojure.test]))

;(deftest identities
; (is (= (normalize '(a)) '(a)))
; (is (= (normalize '(stencil program (a))) '(stencil program (a)))))
