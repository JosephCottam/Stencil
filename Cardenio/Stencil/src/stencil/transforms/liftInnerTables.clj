'(in-ns 'stencil.transforms)


(defn liftInnerTables 
  "Tables can be nested in the raw syntax BUT its easier if they aren't in the end.
   Name-mangles and lifts out tables."
  [program]
  program)
